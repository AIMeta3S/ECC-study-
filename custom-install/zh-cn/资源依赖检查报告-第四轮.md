# 资源依赖检查报告 — 第四轮

> 检查对象：第三轮补齐的 **11 个新增文件**（2 hook + 6 lib + 3 skill），逐个提取引用/依赖，判定在 `custom-install/zh-cn/` 下是否存在。
> 检查方法：8 个 `.js` 用脚本提取静态相对 `require()` + 路径解析 + 比对基准；3 个 `SKILL.md` 用 agent 精读；逐项独立核对根目录存在性。
> 收敛进度：32（第二轮）→ 11（第三轮）→ **11（本轮）**。

## 缺失资源清单（共 11 项）

| # | 缺失资源 | 类型 | 引用方（zh-cn 内文件） | 根因 |
|---|---|---|---|---|
| 1 | `scripts/hooks/auto-tmux-dev.js` | script | [scripts/hooks/bash-hook-dispatcher.js](custom-install/zh-cn/scripts/hooks/bash-hook-dispatcher.js) | 漏拷贝（根存在）— `require('./auto-tmux-dev')` |
| 2 | `scripts/hooks/block-no-verify.js` | script | [scripts/hooks/bash-hook-dispatcher.js](custom-install/zh-cn/scripts/hooks/bash-hook-dispatcher.js) | 漏拷贝（根存在）— `require('./block-no-verify')` |
| 3 | `scripts/hooks/post-bash-build-complete.js` | script | [scripts/hooks/bash-hook-dispatcher.js](custom-install/zh-cn/scripts/hooks/bash-hook-dispatcher.js) | 漏拷贝（根存在）— dispatcher 子模块 |
| 4 | `scripts/hooks/post-bash-command-log.js` | script | [scripts/hooks/bash-hook-dispatcher.js](custom-install/zh-cn/scripts/hooks/bash-hook-dispatcher.js) | 漏拷贝（根存在）— dispatcher 子模块 |
| 5 | `scripts/hooks/post-bash-pr-created.js` | script | [scripts/hooks/bash-hook-dispatcher.js](custom-install/zh-cn/scripts/hooks/bash-hook-dispatcher.js) | 漏拷贝（根存在）— dispatcher 子模块 |
| 6 | `scripts/hooks/pre-bash-commit-quality.js` | script | [scripts/hooks/bash-hook-dispatcher.js](custom-install/zh-cn/scripts/hooks/bash-hook-dispatcher.js) | 漏拷贝（根存在）— dispatcher 子模块 |
| 7 | `scripts/hooks/pre-bash-git-push-reminder.js` | script | [scripts/hooks/bash-hook-dispatcher.js](custom-install/zh-cn/scripts/hooks/bash-hook-dispatcher.js) | 漏拷贝（根存在）— dispatcher 子模块 |
| 8 | `scripts/hooks/pre-bash-tmux-reminder.js` | script | [scripts/hooks/bash-hook-dispatcher.js](custom-install/zh-cn/scripts/hooks/bash-hook-dispatcher.js) | 漏拷贝（根存在）— dispatcher 子模块 |
| 9 | `scripts/lib/package-manager.js` | script | [scripts/lib/resolve-formatter.js](custom-install/zh-cn/scripts/lib/resolve-formatter.js) | 漏拷贝（根存在）— `require('./package-manager')` |
| 10 | `swift-actor-persistence` | skill | [skills/swiftui-patterns/SKILL.md](custom-install/zh-cn/skills/swiftui-patterns/SKILL.md#L258) | 漏拷贝（根存在）— "See skill: swift-actor-persistence" |
| 11 | `swift-protocol-di-testing` | skill | [skills/swiftui-patterns/SKILL.md](custom-install/zh-cn/skills/swiftui-patterns/SKILL.md#L259) | 漏拷贝（根存在）— "See skill: swift-protocol-di-testing" |

## 关键发现

1. **`bash-hook-dispatcher.js` 是 Bash hook 的中枢 dispatcher**：它一次性 require 了 8 个 sub-hook（auto-tmux-dev、block-no-verify、4 个 post-bash-* / pre-bash-* 子规则），构成本轮 8/9 的 script 缺失。这些 sub-hook 拷贝后，自身还可能 require 进一步模块 → 延续至第五轮。

2. **agent 误判已由独立核对纠正**：agent 初判第 10/11 项（swift-actor-persistence、swift-protocol-di-testing）为"悬空引用（文件系统不存在）"；独立核对根目录后均为 `ROOT=Y`，实为漏拷贝。与第二轮 `typescript`（真悬空）形成对照——agent 的"存在性"判断一律以独立 `os.path.exists` 核对为准。

3. **附带发现（非本轮范围）**：第 10/11 项同名还登记在 [config/project-stack-mappings.json](custom-install/zh-cn/config/project-stack-mappings.json)（L284/L285）中。该 config 不是本轮新增文件，不在检查对象内，但补齐这两个 skill 后，config 与实际产物将一致。

4. **无缺失文件**：`design-system`（`/design-system` 为 skill 自引用，已存在）、`liquid-glass-design`（自包含，仅引用 Apple 平台外部技术）经查无 ECC 资源缺失。

## 附录：8 个 `.js` 的相对 require（依据）

| 文件 | 相对 require |
|---|---|
| bash-hook-dispatcher.js | `../lib/hook-flags`✓ `./auto-tmux-dev`✗ `./block-no-verify`✗ `./gateguard-fact-force`✓ `./post-bash-build-complete`✗ `./post-bash-command-log`✗ `./post-bash-pr-created`✗ `./pre-bash-commit-quality`✗ `./pre-bash-git-push-reminder`✗ `./pre-bash-tmux-reminder`✗ `./pretooluse-visible-output`✓ |
| pretooluse-visible-output.js | — |
| hook-flags.js | — |
| llm-summary.js | `./transcript-context`✓ |
| observer-sessions.js | `./utils`✓ |
| resolve-formatter.js | `./package-manager`✗ |
| shell-substitution.js | — |
| transcript-context.js | — |

## 收敛展望

本轮缺失仍 11 项，但结构从"lib 工具函数"转向"`bash-hook-dispatcher` 的 sub-hook 群 + 2 个 swift skill"。预期第五轮拷贝后，sub-hook 与 package-manager 的 require 链基本到顶（剩余 lib 工具已大量补齐），有望收敛至个位数。
